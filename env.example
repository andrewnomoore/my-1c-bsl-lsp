# идентификатор проекта
MCP_PROJECT_NAME=do

# префикс докер-контейнера. Итоговое имя = Префикс + Идентификатор
MCP_CONTAINER_PREFIX=mcp-lsp

# используем latest, что бы при пересборке обновлялся BSL SL
MCP_LSP_BRIDGE_TAG=latest

# путь к каталогу проекта 1С, с которым будет работать этот экземпляр МСР 
HOST_PROJECTS_ROOT=F:\ame_co\sukhov_ae\1c-workflow\workspace\do-project

# под каким именем маунтится каталог проекта внутри контейнера
PROJECTS_ROOT=/projects

# Каталоги с кодом 1С (workspace roots)
# Это нужно для работы с основной конфигурацией и расширениями одновременно
#
# Примеры:
# Один каталог:
#   WORKSPACE_ROOT=/projects/main-config
#
# Основная конфигурация + расширение (что бы воркспейс рут захватил оба каталога с кодом):
#   WORKSPACE_ROOT=/projects
#
WORKSPACE_ROOT=/projects

# Volume mode: ro or rw (rw нужен, что бы BSL LS мог редактировать код - операции переименования и другие)
PROJECTS_MOUNT_MODE=rw

# Container resources (огранчиения докер-контейнера. 
# Должны быть чуть выше чем ресурсы BSL LS, т.к. в контейнере живет не только он). 
# Можно отключить для динамического потребления 
#CONTAINER_MEMORY=8g
#CONTAINER_CPUS=4

# Настройки запуска BSL LS. Потребление памяти зависит от размера проекта. 
# Если памяти не хватит - начнет сильно тупить. Лучше не жадничать
# По настройке BSL LS можно задать вопросы в канале Телеграм https://t.me/bsl_language_server
#Верхний потолок памяти
MCP_LSP_BSL_JAVA_XMX=6g
#Стартовый уровень памяти
MCP_LSP_BSL_JAVA_XMS=2g

# Logging (debug, info, warn, error)
MCP_LSP_LOG_LEVEL=error

# MCP транспорт: stdio (по умолчанию, через docker exec) или http (Streamable HTTP на порту)
# stdio - клиент подключается через: docker exec <container> mcp-lsp-bridge
# http  - клиент подключается по HTTP: http://localhost:<port>/mcp
#MCP_TRANSPORT=stdio

# Порт HTTP-сервера MCP (используется только при MCP_TRANSPORT=http)
#MCP_HTTP_PORT=8080

